
## don't ask if https or ssh with usethis::use_github()
options(usethis.protocol = "https")

## Default repo
local({r = getOption("repos")
       r["CRAN"] = "https://cloud.r-project.org" 
       options(repos=r)
 })

